# 导入数据文件对话框选择问题修复方案

## 问题概述

在 macOS 上快速点击"导入数据文件"按钮时，文件选择对话框可能出现无法选择文件的问题。问题原因是缺少防重复点击保护机制。

## 问题根源分析

### 1. 缺少防重复点击保护
- `load_btn_click` 方法没有任何保护措施
- 用户可以快速多次点击，导致创建多个 `QFileDialog` 实例

### 2. `_is_loading_new_data` 设置时机太晚
- 在经过大量文件格式检测后才在 `_load_file` 中设置
- 在此之前用户可以重复点击按钮

### 3. 按钮状态管理不完整
- `set_button_status` 方法没有控制 `load_btn` 的启用/禁用状态

## 推荐修复方案

### 方案概览

采用**多层次防护**策略，从三个维度同时进行修复：

1. **按钮级防护**：点击后立即禁用按钮
2. **状态级防护**：尽早设置加载状态标志
3. **对话框级防护**：对话框显示期间阻止再次触发

### 修改内容

#### 1. 修改 `load_btn_click` 方法

**文件**：[csv_plot_pyqt6.py](file:///Users/xiaolin/Documents/python_repo/csv_plot/csv_plot_pyqt6.py#L4874-L4886)

**原代码**：
```python
def load_btn_click(self):
    initial_dir = self._get_dialog_initial_directory()
    dialog = QFileDialog(self, "选择数据文件")
    dialog.setFileMode(QFileDialog.FileMode.ExistingFile)
    dialog.setAcceptMode(QFileDialog.AcceptMode.AcceptOpen)
    dialog.setNameFilter("all File (*.*);;CSV File (*.csv);;m File (*.mfile);;t00 File (*.t00);;t01 File (*.t01);;t10 File (*.t10);;t11 File (*.t11)")
    if initial_dir:
        dialog.setDirectory(initial_dir)

    if dialog.exec():
        selected_files = dialog.selectedFiles()
        if selected_files:
            self.load_csv_file(selected_files[0])
```

**修改后**：
```python
def load_btn_click(self):
    # 防护1：检查是否正在加载
    if getattr(self, "_is_loading_new_data", False):
        return
    
    # 防护2：立即禁用按钮
    self.load_btn.setEnabled(False)
    
    try:
        initial_dir = self._get_dialog_initial_directory()
        dialog = QFileDialog(self, "选择数据文件")
        dialog.setFileMode(QFileDialog.FileMode.ExistingFile)
        dialog.setAcceptMode(QFileDialog.AcceptMode.AcceptOpen)
        dialog.setNameFilter("all File (*.*);;CSV File (*.csv);;m File (*.mfile);;t00 File (*.t00);;t01 File (*.t01);;t10 File (*.t10);;t11 File (*.t11)")
        if initial_dir:
            dialog.setDirectory(initial_dir)

        if dialog.exec():
            selected_files = dialog.selectedFiles()
            if selected_files:
                self.load_csv_file(selected_files[0])
            else:
                # 用户取消选择，恢复按钮状态
                self.load_btn.setEnabled(True)
        else:
            # 用户取消对话框，恢复按钮状态
            self.load_btn.setEnabled(True)
    except Exception:
        # 发生异常也要恢复按钮状态
        self.load_btn.setEnabled(True)
        raise
```

#### 2. 修改 `set_button_status` 方法

**文件**：[csv_plot_pyqt6.py](file:///Users/xiaolin/Documents/python_repo/csv_plot/csv_plot_pyqt6.py#L5026-L5035)

**原代码**：
```python
def set_button_status(self,status:bool):
    if status is not None:
        self.time_correction_btn.setEnabled(status)
        #self.reload_btn.setEnabled(status)
        self.clear_all_plots_btn.setEnabled(status)
        self.auto_range_btn.setEnabled(status)
        self.auto_y_btn.setEnabled(status) 
        self.cursor_btn.setEnabled(status)
        self.mark_region_btn.setEnabled(status)
        self.grid_layout_btn.setEnabled(status)
```

**修改后**：
```python
def set_button_status(self,status:bool):
    if status is not None:
        self.load_btn.setEnabled(status)  # 新增：控制导入按钮
        self.time_correction_btn.setEnabled(status)
        #self.reload_btn.setEnabled(status)
        self.clear_all_plots_btn.setEnabled(status)
        self.auto_range_btn.setEnabled(status)
        self.auto_y_btn.setEnabled(status) 
        self.cursor_btn.setEnabled(status)
        self.mark_region_btn.setEnabled(status)
        self.grid_layout_btn.setEnabled(status)
```

#### 3. 修改 `_end_data_reload` 方法

**文件**：[csv_plot_pyqt6.py](file:///Users/xiaolin/Documents/python_repo/csv_plot/csv_plot_pyqt6.py#L4961-L4983)

确保在数据加载结束后恢复按钮状态，保持与 `set_button_status` 的协调。

**修改建议**：
在 `_end_data_reload` 中，不需要额外操作，因为：
- 如果加载成功，`_post_load_actions` 会调用 `set_button_status(True)`
- 如果加载失败，异常处理中应该确保按钮状态恢复

#### 4. 补充：在 `load_csv_file` 中增加防护

**文件**：[csv_plot_pyqt6.py](file:///Users/xiaolin/Documents/python_repo/csv_plot/csv_plot_pyqt6.py#L4994-L5024)

在 `load_csv_file` 开头增加检查：

```python
def load_csv_file(self, file_path: str):
    """
    加载CSV文件
    """
    # 防护：再次检查加载状态
    if getattr(self, "_is_loading_new_data", False):
        debug_log("MainWindow.load_csv_file skipped - already loading")
        return
        
    debug_log("MainWindow.load_csv_file start path=%s is_loading=%s",
              file_path, getattr(self, "_is_loading_new_data", False))
    if not self._validate_file_path(file_path):
        self.load_btn.setEnabled(True)  # 恢复按钮
        return
        
    if not self._check_file_size(file_path):
        self.load_btn.setEnabled(True)  # 恢复按钮
        return
    # ... 其余代码不变
```

#### 5. 补充：在 `_on_load_done` 和 `_on_load_error` 中恢复按钮

确保异步加载完成或出错时，按钮状态能够恢复。

## 修复方案特点

### 优点
1. **多重防护**：从按钮禁用、状态检查、异常恢复三个层面保障
2. **用户体验好**：按钮状态清晰，用户知道何时可以操作
3. **代码侵入小**：修改集中在相关方法，不影响整体架构
4. **兼容性好**：保持现有功能不变，只增加防护逻辑

### 防护层级说明
1. **第一层**：`load_btn_click` 入口检查 + 立即禁用按钮
2. **第二层**：`load_csv_file` 再次检查加载状态
3. **第三层**：`_begin_data_reload` 设置的 `_is_loading_new_data` 标志
4. **恢复层**：对话框取消/加载完成/异常时均恢复按钮状态

## 验证要点

修复后请验证以下场景：
1. ✅ 快速多次点击"导入数据文件"按钮，只弹出一个对话框
2. ✅ 在对话框显示期间，按钮保持禁用状态
3. ✅ 取消对话框后，按钮恢复可用
4. ✅ 选择文件加载成功后，按钮恢复可用
5. ✅ 加载出错时，按钮也能恢复可用
6. ✅ 不影响其他功能的正常使用

## 备选简化方案

如果觉得上面方案太复杂，可以采用简化版本：

**仅修改 `load_btn_click`**：
```python
def load_btn_click(self):
    # 使用 QSignalBlocker 临时阻止按钮信号
    from PyQt6.QtCore import QSignalBlocker
    
    with QSignalBlocker(self.load_btn):
        initial_dir = self._get_dialog_initial_directory()
        dialog = QFileDialog(self, "选择数据文件")
        # ... 其余代码不变
```

但推荐使用完整方案，防护更全面。
